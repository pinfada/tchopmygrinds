<!DOCTYPE html>
<html lang="fr">
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="TchopMyGrinds - Marketplace moderne pour trouvez des vendeurs et produits locaux près de chez vous avec géolocalisation.">
    <meta name="keywords" content="Marketplace, cameroun, e-commerce, géolocalisation, produits locaux">
    
    <!-- Basic CSS for non-React pages only -->
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body>
    <!-- Flash messages -->
    <% flash.each do |key, value| %>
      <div class="flash-message <%= key %>">
        <%= value %>
      </div>
    <% end %>
    
    <!-- Contenu principal -->
    <main>
      <%= yield %>
    </main>
  </body>
</html>